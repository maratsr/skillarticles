<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:id="@+id/scroll"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior">
    <!--    Для взаимодействия с NestedScrollView для TextView выставим focusable и focusableInTouchMode=true
    saveenabled=false - чтобы не переполнить контент (не сохранять в бандл)
    clipToPadding=false - чтобы не обрезали паддинги
    -->
    <ru.skillbranch.skillarticles.ui.custom.markdown.MarkdownContentView
        android:id="@+id/tv_text_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:focusable="true"
        android:clipToPadding="false"
        android:focusableInTouchMode="true" />

</androidx.core.widget.NestedScrollView>